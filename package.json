{
  "name": "java-cap-sample-cds",
  "version": "1.0.0",
  "description": "Generated by cds-services-archetype",
  "license": "ISC",
  "repository": "<Add your repository here>",
  "sapux": [
    "app/project1"
  ],
  "scripts": {
    "watch": "cd srv && mvn cds:watch",
    "start": "mvn spring-boot:run",
    "build": "npx -p mbt mbt build --mtar archive",
    "deploy": "cf deploy mta_archives/archive.mtar --retries 1",
    "clean": "rimraf mta_archives && mvn clean",
    "start-local:project1": "npm run start:local --prefix app/project1"
  },
  "devDependencies": {
    "@sap/cds-dk": "^7",
    "@sap/ux-specification": "^1.120.1",
    "rimraf": "^5.0.5"
  }
}
